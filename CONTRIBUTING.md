# 기여하기 — pqcaton

이슈·PR 환영한다. 다만 이 리포는 **듀얼 라이선스**라(→ [LICENSING](LICENSING.md)) 코드 기여에
한 가지 절차가 더 붙는다. 먼저 읽어 주십사 한다.

## 기여자 라이선스 동의(CLA)

**코드를 보내기 전에 CLA에 동의해야 한다.** 첫 PR에 봇이 안내한다.

### 왜 필요한가

pqcaton은 같은 코드를 AGPL-3.0과 상업 라이선스 **두 가지로** 낸다. 그러려면 그 코드를 두
라이선스로 낼 권리가 있어야 한다.

당신이 보낸 코드가 AGPL로만 존재하면, **그 부분은 상업 라이선스에 넣을 수 없다.** 한 줄만
섞여도 그 파일이 막힌다. 나중에 소급하려면 기여자 전원의 동의를 다시 받아야 하고, 한 사람과
연락이 닿지 않으면 그 코드를 들어내는 수밖에 없다. 그래서 **처음부터** 받는다.

### 무엇에 동의하나

- 보낸 코드가 **당신 것**이거나, 낼 권한이 있다
- 회사 소속이면 **회사가 허락**했다 (고용 계약이 코드를 회사에 귀속시키는 경우가 많다)
- (주)에스앤티소프트가 그 코드를 **AGPL-3.0과 상업 라이선스 양쪽으로** 낼 수 있다

**저작권은 당신에게 남는다.** 양도가 아니라 허락이다. 당신은 자기 코드를 다른 곳에 자유롭게
쓸 수 있다.

동의하기 어려우면 코드 대신 **이슈로 알려주십사 한다** — 재현 방법과 기대한 동작만으로도
충분히 도움이 된다.

## 의존성을 추가할 때

**허용적 라이선스만 받는다** — MIT · Apache-2.0 · BSD 계열. GPL·LGPL·AGPL 라이브러리는
링크할 수 없다.

이유는 라이선스 취향이 아니다. **상업 라이선스로 줄 수 있는 것은 그렇게 라이선스할 수 있는
코드뿐이다.** 카피레프트 라이브러리를 링크하면 전체를 비공개 조건으로 낼 수 없어, 듀얼
라이선스가 그 자리에서 무너진다.

CI가 이것을 검사한다(`make check-licenses`). 게이트에 걸리면 대안을 찾거나, 별도 프로세스로
분리해 호출하는 방법을 이슈에서 상의한다.

## 상류(pqcota)에 관한 것

관측·정규화·전환물 생성은 [pqcota](https://github.com/randyinthedev-hash/pqcota)가 한다.
그쪽 문제나 계약(`contracts/`) 변경이 필요하면 **pqcota 리포에 직접 이슈를 연다.** 여기서
받아 대신 전달하지 않는다 — 그 리포는 소유 주체가 다르고, 공개 절차로 논의되어야 다른
소비자에게도 같은 문이 열린다.

## 커밋과 PR

- 커밋 메시지는 **무엇을 왜 바꿨는지**를 적는다. 한국어·영어 무관하다
- PR 하나에 한 가지 변경. 리뷰가 가능한 크기로 나눈다
- 테스트가 있는 코드를 고치면 테스트도 함께 고친다

## 문의

- 버그·제안 → [이슈](https://github.com/sntsoftgit/pqcaton/issues)
- 상업 라이선스 → **kty@sntsoft.co.kr**
- 보안 취약점 → 이슈로 열지 말고 **kty@sntsoft.co.kr**로 알려주십사 한다
