# 기여하기 — pqcaton

이슈·PR 환영합니다. 다만 이 리포는 **소스 공개형 상용 라이선스**(BUSL-1.1)라
(→ [LICENSING](LICENSING.md)) 코드 기여에 한 가지 절차가 더 붙습니다. 먼저 읽어 주십시오.

## 기여자 라이선스 동의(CLA)

**코드를 보내시기 전에 CLA에 동의해야 합니다.** 첫 PR에 봇이 안내합니다.

### 왜 필요한가

pqcaton은 **각 릴리스가 공개일로부터 4년 뒤 Apache-2.0으로 전환**됩니다. 라이선스가 그렇게 약속하고
있으므로, 회사는 그날 모든 코드를 Apache-2.0으로 낼 권리가 있어야 합니다.

보내신 코드에 그 권리가 없으면 **그 약속을 지킬 수 없습니다.** 한 줄만 섞여도 그 파일이
막힙니다. 나중에 소급하려면 기여자 전원의 동의를 다시 받아야 하고, 한 사람과 연락이 닿지
않으면 그 코드를 들어내는 수밖에 없습니다. 그래서 **처음부터** 받습니다.

상업 라이선스 계약도 같은 권리를 전제로 합니다.

### 무엇에 동의하나

- 보내신 코드가 **본인 것**이거나, 낼 권한이 있다
- 회사 소속이면 **회사가 허락**했다 (고용 계약이 코드를 회사에 귀속시키는 경우가 많습니다)
- (주)에스앤티소프트가 그 코드를 **BUSL-1.1로, 그리고 Change Date 이후 Apache-2.0으로** 낼 수 있다

**저작권은 기여자에게 남습니다.** 양도가 아니라 허락입니다. 자기 코드를 다른 곳에 자유롭게
쓰실 수 있습니다.

동의하기 어려우시면 코드 대신 **이슈로 알려 주십시오** — 재현 방법과 기대한 동작만으로도
충분히 도움이 됩니다.

## 의존성을 추가할 때

**허용적 라이선스만 받습니다** — MIT · Apache-2.0 · BSD 계열. GPL·LGPL·AGPL 라이브러리는
링크할 수 없습니다.

이유는 라이선스 취향이 아닙니다. 카피레프트 라이브러리를 링크하면 **파생물 전체가 그 조건을
따라야 해서**, BUSL로 내는 것도 Change Date에 Apache-2.0으로 전환하는 것도 막힙니다.

**Go 모듈만이 아닙니다.** 화면이 브라우저로 내보내는 `.js`·`.css`도 같은 게이트를 지납니다 —
바이너리에 박혀 나가므로 배포물의 일부입니다. 파일을 하나 받아 넣었다면 `licenses.txt`에
경로와 SPDX 식별자를 적고, **근거가 된 LICENSE 원문을 그 파일 옆에 함께 둡니다**
(`pkg/inventory/ui/static/LICENSE.htmx`가 그 예입니다). 우리가 쓴 파일이면 `BUSL-1.1`입니다.

CDN 링크로 대신하지 마십시오. 망이 끊긴 기계에서 화면이 깨지고, 남의 서버에서 오는
스크립트는 **이 게이트가 볼 수조차 없습니다.**

CI가 이것을 검사합니다(`make check-licenses`). 게이트에 걸리면 대안을 찾거나, 별도 프로세스로
분리해 호출하는 방법을 이슈에서 상의합니다.

## 화면(`.templ`)을 고칠 때

템플릿은 [templ](https://templ.guide)로 쓰고, **생성물(`*_templ.go`)을 리포에 함께 둡니다.**
그래서 빌드에는 생성기가 필요 없고, 이 리포를 `go get` 하는 쪽도 도구를 깔지 않습니다.

`.templ`을 고쳤으면:

```
make generate          # *_templ.go 를 다시 만든다
make verify-generated  # 고쳐 놓고 안 돌린 날을 잡는다
```

생성물을 **같은 커밋에** 넣습니다. 빠뜨리면 소스는 새것인데 화면은 예전 것이 떠서,
무엇을 보고 있는지가 어긋납니다.

## 기반 프로젝트(pqcota)에 관한 것

관측·정규화·전환물 생성은 [pqcota](https://github.com/randyinthedev-hash/pqcota)가 합니다.
그쪽 문제나 계약(`contracts/`) 변경이 필요하면 **pqcota 리포에 직접 이슈를 열어 주십시오.**
여기서 받아 대신 전달하지 않습니다 — 그 리포는 소유 주체가 다르고, 공개 절차로 논의되어야
다른 소비자에게도 같은 문이 열립니다.

## 커밋과 PR

- 커밋 메시지는 **무엇을 왜 바꿨는지**를 적습니다. 한국어·영어 무관합니다
- PR 하나에 한 가지 변경. 리뷰가 가능한 크기로 나눕니다
- 테스트가 있는 코드를 고치면 테스트도 함께 고칩니다

## 문의

- 버그·제안 → [이슈](https://github.com/sntsoftgit/pqcaton/issues)
- 상업 라이선스 → **kty@sntsoft.co.kr**
- 보안 취약점 → 이슈로 열지 마시고 [SECURITY.md](SECURITY.md)의 경로로 알려 주십시오
