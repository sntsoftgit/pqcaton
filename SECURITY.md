# 보안 정책 (Security Policy)

pqcaton은 조직의 암호 자산 인벤토리를 **선언과 대조하고, 사람이 확정한 판정만 적용**하는
거버넌스 도구입니다. 러너는 **고객 서버 안에서 고객 권한으로** 돌고, 다루는 것은 어느 노드가
무엇을 쓰는가가 곧 그 조직의 공격면 그 자체입니다. 그래서 보안 신고를 진지하게 받습니다.

## 지원 버전

| 버전 | 보안 수정 |
|---|---|
| **최신 릴리스** · `main` | ✅ |
| 그 이전 릴리스 | **백포트하지 않습니다** |

**수정은 `main`에 들어가고 다음 릴리스로 나갑니다.** 옛 버전에 따로 패치를 내지 않습니다.
1인 개발이라 여러 줄기를 함께 유지하면 어느 줄기도 제때 고치지 못합니다. 쓰시는 쪽에서는
**최신 릴리스로 올리는 것이 보안 수정을 받는 길입니다.**

각 릴리스가 4년 뒤 Apache-2.0으로 전환되지만(→ [LICENSING](LICENSING.md)), 그것은 **라이선스가
열리는 것이지 그 버전을 계속 고쳐 준다는 뜻이 아닙니다.**

## 취약점 신고

**공개 이슈·PR·토론으로 올리지 말아 주십시오.** 다음 비공개 경로로 알려 주십시오:

1. **GitHub 비공개 신고 (권장)**: 이 리포의 **Security 탭 → "Report a vulnerability"**.
2. 위가 어려우면 **kty@sntsoft.co.kr** 로 보내 주십시오. 제목에 `[security]`를 달아 주시면 빨리 눈에 띕니다.

영향 컴포넌트(러너 / 대조 엔진 / CLI), 재현 절차, 영향(정보 노출·권한 상승·무결성 훼손),
가능하면 PoC를 담아 주시면 분류가 빠릅니다.

> **취약점이 아닌 것은 공개 이슈가 맞습니다.** 버그·질문·제안은 숨길 이유가 없고, 공개된
> 논의가 다음 사람에게 남습니다. 비공개로 받는 것은 **고치기 전에 알려지면 사용자가 공격당할
> 수 있는 것**뿐입니다.

## 응대

1인 개발이라 **best-effort**입니다. 접수 확인, 재현·평가 뒤 수정 계획을 신고자와 조율합니다.
SLA는 두지 않되 유효한 신고는 먼저 처리합니다.

## 이 프로젝트에서 특히 보안 이슈로 보는 것

러너가 무엇을 보내고 무엇을 보내지 않는지는 [saas/runner/README.md](saas/runner/README.md)에
표로 있습니다. **그 표가 깨지는 경로가 곧 취약점입니다.**

- **러너 토큰 유출**: 토큰은 헤더로만 나가고 URL·오류 메시지·로그에 담기지 않습니다.
  어디로든 새면 취약점입니다. 토큰 하나가 조직 하나이므로 새면 그 조직이 통째로 열립니다.
- **주소 유출**: 등재는 접속 주소를 조직 키 HMAC 토큰으로 바꾸고 **원본을 버립니다.** 원본
  주소나 접속에 쓸 수 있는 값이 밖으로 나가거나 어딘가에 남으면 취약점입니다.
- **조직 격리 파괴**: 한 조직의 러너가 다른 조직의 데이터를 읽거나 쓰게 되는 경로.
- **판정 관문 우회**: `finalized`가 아닌 판정이 계획으로 나가는 경로, 판정 원장(append-only)의
  위·변조를 받아들이는 것, 근거가 달라졌는데도 옛 판정이 유효하게 남는 것이 모두 여기 듭니다.
- **생성물 주입**: 관측·선언 입력이 기반 프로젝트 pqcota의 플레이북·config로 악성 내용으로 흘러드는 경로.

## 범위 밖

- **기반 프로젝트 pqcota**(collector·CLI·계약)의 문제는
  [pqcota의 보안 정책](https://github.com/randyinthedev-hash/pqcota/blob/main/SECURITY.md)을
  따라 주십시오.
- 운영자가 직접 돌리는 플레이북의 **실행 환경**(권한 설정·네트워크 구성)은 실행 측 책임입니다.

범위가 애매하면 판단하지 마시고 그냥 신고해 주십시오. 함께 논의하겠습니다.
