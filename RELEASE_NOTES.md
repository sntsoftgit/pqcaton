# 릴리스 노트 — pqcaton

[개요](README.md) · **릴리스 노트** · [여정](docs/journey.md) · [설계](docs/design.md) · [검증 기준](docs/testcases.md) · [데모](demo/README.md) · [구조 그림](https://www.sntsoft.co.kr/pqcaton/)

버전별 **목표**와 **성과**를 기록합니다. 버전이 올라갈 때마다 갱신하고, 최신 버전을 맨 위에 둡니다.

절은 내용의 **종류**로 나눕니다 — **만든 것** · **알아낸 것** · **고친 것**. 결함은 기능으로
포장하지 않고 자기 절에 적습니다. 그 절은 세 가지를 밝힙니다: **무엇이 잘못이었나 · 어느
버전부터인가 · 무엇이 잘못 나왔나**(아무것도 안 틀렸으면 그렇게 적습니다). **발행된 버전의
절은 고치지 않습니다** — 결함은 그것을 고친 버전에서 어디서부터였는지를 밝히는 쪽으로 남깁니다.

> 이 규칙은 [pqcota의 릴리스 노트](https://github.com/randyinthedev-hash/pqcota/blob/main/RELEASE_NOTES.md)와
> 같은 것입니다. 두 리포를 오가며 읽는 사람이 같은 자리에서 같은 것을 찾게 하기 위해서입니다.

**문구는 발행 뒤에도 고칩니다.** 「발행된 절은 고치지 않는다」는 **무엇을 만들었고 무엇이
틀렸다고 말했는지**를 지키는 규칙이지, 어색한 한국어를 그대로 두라는 규칙이 아닙니다.
읽히지 않는 문장은 기록이 아니라 잡음입니다 — 주장은 한 글자도 바꾸지 않고 말만 고칩니다.

**발행하지 않은 절이 있습니다.** 절은 착수할 때도 세우므로, 그 사이에 다음 버전이 나가면
태그 없이 남습니다. 그런 절은 제목에 「발행 안 함」을 달고 어느 릴리스에 함께 나갔는지를
맨 위에 적습니다 — **묶어 지우지 않는 것**은 무엇을 왜 만들었는지가 한 덩어리가 되면
읽히지 않기 때문입니다.

**각 버전의 Apache-2.0 전환일을 함께 적습니다.** 라이선스가 릴리스일로부터 4년으로 정해져
있어([LICENSING.md](LICENSING.md)), 버전마다 열리는 날이 다릅니다 — 어느 버전을 쓰든 언제
열리는지 여기서 확인하십시오. 이 줄은 **이미 발행된 절에도 나중에 더했습니다.** 위 규칙의
예외이고, 그렇게 한 이유는 하나입니다 — 라이선스가 이 문서를 전환일의 근거로 가리키는데
옛 절에만 없으면 그 버전을 쓰는 사람이 답을 못 찾습니다. **경위가 아니라 라이선스 사실을
더한 것이라, 그 절이 무엇을 만들고 무엇이 틀렸다고 말했는지는 한 글자도 바꾸지 않았습니다.**

> **§ 표기**: 별도 언급이 없으면 pqcota
> [규정서](https://github.com/randyinthedev-hash/pqcota/blob/main/docs/regulation.md)의 절 번호입니다.

---

## 로드맵 — 예정 릴리스 (계획)

확정이 아니라 방향입니다. 각 버전은 착수·완료 시 위 규칙대로 정식 절로 승격합니다.

**화면의 경계를 정했습니다** — *한 대의 기계에서 파일로 끝나는 것은 pqcaton, 여러 기계 ·
여러 사람 · 시간에 걸친 것은 컨트롤 플레인.* 라이선스가 **노드 수로만** 가르므로(§Additional
Use Grant), 기능을 빼서 무료판을 약하게 만들지 않습니다. 5노드 사용자가 **터미널 없이 한
바퀴를 돌 수 있는 것**이 목표입니다 — v0.12.0에서 이뤘습니다.

올릴 것은 **이미 전부 명령으로 있습니다** — 새로 만드는 것이 아니라 화면을 붙이는 일입니다.
입력하는 자리부터 갑니다. 지금 가장 아픈 곳이 CSV·JSON을 손으로 쓰는 자리입니다.


**화면에 두지 않는 것**

| | 어디 | 왜 |
|---|---|---|
| **접속 정보** — 주소 · 계정 · 키 | pqcota | 경계가 아니라 **원칙**입니다. 접속 비밀은 런타임 전용 파일에만 두고 인벤토리에 영속하지 않습니다(§1.5) — 화면에 올리면 그 원칙이 깨집니다 |
| 러너 등재 · 상태 · 영역 | 컨트롤 플레인 | 여러 기계 |
| 조직 · 사용자 · 권한 | 컨트롤 플레인 | 여러 사람 |
| 판정 이력 조회 · 드리프트 · 알림 | 컨트롤 플레인 | 시간 |

**「관측 대상 노드」는 둘로 갈립니다.** 노드 이름·IP·등재 목록(스코프 마스터)은 선언 파일에
있으므로 **위에 있습니다.** 접속 정보만 밖입니다.

### 로드맵에 없는 것 — 만들지 않습니다

방향이 아니라 **경계**입니다. 언젠가 오리라 기대하지 않도록 적습니다.

| 만들지 않는 것 | 대신 |
|---|---|
| **관측·수집** | pqcota가 합니다. 이 리포는 그 계약만 소비합니다 |
| **전환물 생성·적용** | pqcota의 `provisioning`이 합니다. 우리는 확정된 계획을 넘길 뿐입니다 |
| **자동 판정** — "이건 위험하니 바꿔라" | 판정 대상을 구조화할 뿐, **확정은 사람**이 합니다([§3.1](https://github.com/randyinthedev-hash/pqcota/blob/main/docs/regulation.md)) |
| **UNOBSERVED의 기계 확정** | 실재하는데 못 본 것인지 이미 없어진 것인지는 사람만 압니다 |

---

## v0.15.0 — 화면은 적는 자리다 (2026-08-21)

**Apache-2.0 전환: 2030-08-21** (릴리스일로부터 4년)

**목표** — 화면을 실제로 써 봤습니다. 두 가지가 드러났습니다. 하나는 **설명이 화면을
덮고 있었다**는 것 — 날마다 여는 자리인데 절마다 문단이 서 있어 적을 칸이 아래로
밀렸습니다. 다른 하나는 **이름이 서로 다르면 대조 결과가 오류 없이 틀린다**는
것입니다. 노드 이름·컴포넌트 이름·런타임 이름이 관측과 한 글자라도 다르면 선언한 것은 미관측으로,
관측된 것은 shadow 로 구분됩니다 — 막히지 않으니 눈으로는 알 수 없습니다.

### 만든 것

- **인벤토리 조회 화면** — 인벤토리에 지금 무엇이 있는지 찾아보는 자리입니다. 노드 ·
  런타임 · 컴포넌트 · 상태로 찾고, 판정 이력과 「근거가 바뀐 판정」을 함께 봅니다.
  **절차의 한 단계가 아닙니다** — 선언 → 스코프 → 대조 → 판정을 거쳐 올 필요 없이
  아무 때나 엽니다. 그래서 탭에 번호를 붙이지 않았습니다.

- **설명을 「도움말」로 접었습니다** — 다섯 화면 전부입니다. 지우지 않고 접습니다.
  `<details>` 하나라 자바스크립트도, 새 화면도 필요 없습니다. 암호 자산 스코프의 「규칙을
  적는 법」은 표째로 들어갑니다. **숫자와 상태는 접지 않습니다** — 「자동통과 후보
  N개」나 「없습니다 — 이 범위에서는 관측이 완전합니다」는 설명이 아니라 결과입니다.

- **선언 화면을 노드마다 한 덩어리로 묶었습니다.** 이름 · IP · 관측 이름 한 줄, 그
  아래 그 노드의 **암호 자산** 표(런타임 · 컴포넌트). 자산은 「이 노드에서 이 모듈이 쓰인다」는
  말이라, 따로 둔 표에서는 노드 이름을 사람이 다시 적어야 했습니다 — 오타 하나면
  선언은 있는데 아무 노드에도 붙지 않는 자산이 됩니다. 폼 이름에도 노드 번호가
  들어갑니다(`asset.<노드>.<줄>.…`).

- **줄을 지우고, 고친 것을 되돌립니다.** 노드 · 암호 자산 · 통신 엣지 줄마다 「제거」가
  있고 **묻고 지웁니다**. 물음에 「저장해야 파일에 반영됩니다」까지 적혀 있습니다 —
  화면에서
  지운 것과 파일에서 지운 것은 다릅니다. 저장 옆의 「다시 불러오기」는 마지막으로
  저장된 선언을 다시 그립니다. 빈 줄은 미리 열지 않습니다 — 적을 것이 있는 사람만
  「추가」로 엽니다.

- **손으로 적을 자리를 줄였습니다.** 런타임은 **목록에서 고릅니다** — 관측 결과에
  나올 수 있는 이름만 들어 있습니다(`reconcile.Runtimes()`). 컴포넌트는 골라 줄 수
  없어 **맞대는 방식을 화면이 말하고**(글자 그대로 같아야 하고, `.so` 뒤는 떼고, 벤더링 해시는 떼지 않는다),
  관측이 있으면 **그 노드에서 관측된 이름이 후보로 뜹니다.** 관측 결과에 적힌
  이름이 곧 맞는 이름입니다.

- **IP를 적은 줄만 관리 대상입니다.** IP가 없으면 관측 주소를 그 이름과 이을 근거가
  없어, 이름만 적어 둔 줄은 관리가 아니라 대조가 어긋나기 시작하는 자리였습니다.
  저장에서 뺍니다 — **뺀 것은 저장 알림에 적힙니다.** 그 노드의 자산도 함께 빠집니다.

- **관측 노드를 선언 노드로 잇습니다.** 봉투에 실려 오는 이름들(대상 노드 id · fqdn ·
  짧은 호스트명 · machine-id)을 선언의 이름과 맞대 봅니다. 그것으로 안 되는 자리는
  선언의 **「관측 이름」**(`observed_as`)에 사람이 적습니다 — 선택 필드라 예전
  파일도 그대로 읽힙니다. 아직 어느 노드에도 붙지 않은 관측 이름은 그 칸에서 고를 수 있고, 겹쳐
  주장하면 짚습니다(IP를 겹쳐 주장하는 것과 같은 자리입니다).

- **탭 이름을 화면이 쓰는 말과 맞췄습니다** — 판정을 **하는** 자리는 「④ 판정(리뷰
  큐)」, **보는** 자리는 「인벤토리·판정 이력」입니다. 화면 문구에 「판정」이 스물몇 번
  나오는데 탭에는 그 말이 없었습니다.

- **상류 pqcota v0.6.3** — v0.6.0이 더한 **Windows CNG 관측이 자산으로 들어옵니다**
  (런타임 `cng` · 컴포넌트 `cng-providers`). 다만 CNG 는 **provider 를 갈아 끼우는
  조치가 아니라서** 계획의 provider 칸이 빕니다 — 갈아 끼울 대상이 관측에 없고, 쓸 수
  있는 알고리즘은 Windows 빌드가 · FIPS 는 OS 정책이 정하며, CNG 의 `fips_validation`
  은 알고리즘 열거로 판별되지 않습니다. 빈 칸을 빠뜨린 자리로 읽지 않게 리뷰 큐가
  「플랫폼 조치」라고 적습니다.

- **말을 다듬었습니다.** `confidence` 를 「확신」에서 **「신뢰도」**로 바꾸고, 화면과
  문서에서 만들어 쓰던 비유 동사를 걷어냈습니다. 화면에서 「자산」은 **「암호 자산」**
  입니다 — 파일 형식의 `assets` 와 pqcota 의 asset policy CSV 는 그대로입니다.

### 알아낸 것

- **설명이 많은 화면은 친절한 것이 아닙니다.** 처음 여는 사람에게 맞춘 문장이 날마다
  여는 사람의 표를 아래로 밉니다. 접어 두면 둘 다 잃는 것이 없습니다 — 지울 이유도, 펼쳐
  둘 이유도 없었습니다.

- **이름이 서로 다르면 아무리 잘 적어도 맞지 않습니다.** 데모에서 관측은 노드를
  `node:48596282fd2faf23` 로, 선언은 `web-gw` 로 불렀습니다. 자산 대조는 이름이 글자
  그대로 같아야 맞으므로 **선언한 자산은 전부 미관측, 관측된 자산은 전부 shadow** 였고,
  컴포넌트를 어떻게 적어도 달라지지 않았습니다. 관측 이름 한 줄을 적자 CONFIRMED 0 →
  1로 바뀌었습니다.

- **지우는 버튼을 넣으면 폼을 읽는 규칙도 함께 바뀝니다.** 화면에서 줄을 지우면 폼의
  번호가 중간에 빕니다 — 끊기는 자리에서 읽기를 멈추던 코드는 지운 줄 **뒤의 것을
  통째로** 버립니다. 넣는 자리만 보고 끝낼 수 없습니다.

- **한국어와 영어는 어순이 달라, 두 말로 옮기고 나서야 드러나는 자리가 있습니다.**
  조회 화면의 「5개 중 2개」가 영어에서는 숫자 둘의 자리가 뒤바뀌어 떴습니다. 한국어
  원문에는 틀릴 자리가 없었습니다.

### 고친 것

- **① 관측 노드 이름이 선언과 다르면 자산 대조가 통째로 어긋났습니다**(v0.1.0~v0.14.0).

  **무엇이 잘못이었나** — 엣지에는 관측 IP를 선언 노드로 잇는 단계가 있는데(§0.4),
  자산에는 그 자리가 없었습니다. collector 는 자기가 붙인 id(`node:<해시>`)나 fqdn 으로
  보내는데, 그것을 그대로 노드 이름으로 썼습니다.

  **무엇이 잘못 나왔나** — 그 노드의 **선언한 자산은 전부 미관측, 관측된 자산은 전부
  shadow** 로 올라옵니다. 오류가 아니라 그럴듯한 결과라, 읽는 사람은 「선언한 것이
  안 쓰이고 있고, 선언에 없는 것이 쓰이고 있다」로 읽습니다.

  **무엇이 바뀌나** — 봉투의 이름들로 잇고, 안 되면 사람이 「관측 이름」을 적습니다.
  **어디에도 안 걸리면 관측이 부른 이름을 그대로 둡니다** — 억지로 고르면 남의 노드
  자산이 붙고, 그 이름으로 shadow 가 올라야 사람이 보고 적어 넣을 수 있습니다.

- **② README 예제에 관측되지 않는 컴포넌트 이름이 적혀 있었습니다**(v0.1.0~v0.14.0).
  `local,jca,provider-chain` 이라고 적혀 있었는데 관측 결과에 나오는 이름은
  `jca-provider-chain` 입니다. 대조는 글자 그대로 같아야 맞으므로, 그 줄은 JVM 이 있는
  머신에서도 영영 미관측입니다 — 예제를 따라 적은 사람은 그것을 「이 머신에 JCA 가
  없다」로 읽습니다.

- **③ 폼 번호가 끊기면 그 뒤가 저장되지 않았습니다.** 「제거」를 넣으며 함께
  고쳤습니다 — 이제 폼에 실제로 온 번호만 차례대로 읽습니다. **발행된 버전에서 잘못
  나온 적은 없습니다**: 그때는 화면이 번호를 촘촘히 매겼고, 줄을 없애는 길이 「이름을
  비우는 것」뿐이었습니다.

- **④ 조회 화면의 「몇 개 중 몇 개」가 두 말에서 뒤집혔습니다.** 한국어는 「전체 중 몇
  개」, 영어는 「몇 개 of 전체」로 어순이 달라 한쪽 말에서만 숫자 둘이 바뀌어 떴습니다
  — 보는 사람은 걸러 낸 것을 전부로 읽습니다. **발행된 버전에서 잘못 나온 적은
  없습니다** — 그 화면과 함께 이 릴리스에서 처음 나갑니다.

---

## v0.14.0 — 손으로 만든 것은 어긋난다 (2026-08-19)

**Apache-2.0 전환: 2030-08-19** (릴리스일로부터 4년)

**목표** — v0.13.0을 낸 뒤 문서를 같은 기준으로 다시 읽었습니다. 코드는 초록인데
**문서와 산출물이 코드를 따라오지 않은 자리**가 남아 있었습니다.

### 고친 것

- **데모 기대 출력이 한국어인 채였습니다.** 명령의 출력을 영어로 옮긴 날
  `demo/expected-output/report.txt` 만 그대로였습니다.

  원인은 스크립트였습니다 — `extend.sh` 가 `pqcaton-report` 를 **돌리기만 하고 그 출력을
  갖고 오지 않았습니다.** `plan.json` 은 갖고 오는데 이것만 아니었고, 그래서 누군가 한 번
  손으로 만들어 둔 파일이었습니다. **손으로 만든 그 순간부터 어긋나기 시작합니다.**
  이제 스크립트가 갖고 옵니다.

- **`plan.json` 이 돌릴 때마다 달라지고 있었습니다.** protojson 은 콜론 뒤 공백을 일부러
  흔듭니다 — 이 리포가 아는 함정인데(v0.4.0), 기대 파일에는 그대로 두고 있었습니다.
  매번 diff 가 더러워져 **진짜 달라진 날을 알아볼 수 없는** 상태였습니다. 갖고 온 뒤
  공백을 고릅니다.

  두 가지를 고친 뒤 데모를 두 번 돌려 **기대 파일이 한 글자도 바뀌지 않는 것**을
  확인했습니다.

- **거절문이 검사하지 않은 것을 말했습니다.** `pqcaton-decide close` 는 서명만 보는데
  「승인자와 서명이 비어 있다」고 했습니다 — 승인자를 채운 사람은 왜 여전히 막히는지
  알 수 없습니다. 코드를 갈랐습니다(`MissingSignature` · `MissingApproval`).

- **README 가 옛 모습을 보여 주고 있었습니다** — 옛 JSON 키(`확정_계획에_넣는다` ·
  `정책_판정`)와, 확정 거절문의 옛 한국어 출력. 실제로 돌려 잡은 것으로 바꿨습니다.

- 리포트 머리 상자의 오른쪽 선이 어긋나 있었습니다 — 글자가 영어로 길어졌는데 여백이
  그대로였습니다.

- 설계 문서의 명령 표에서 `pqcaton-ui` 줄이 「같은 파일을 편집」에 머물러 있었습니다.
  이제 규칙을 고치고 세션까지 엽니다.

### 알아낸 것

- **산출물에도 게이트가 필요합니다.** `make` 는 코드를 지키지만 `demo/expected-output` 은
  아무도 보지 않았습니다. 데모가 무거워(도커·3노드) `make` 에 넣을 수 없는 자리라면,
  **적어도 스크립트가 스스로 갖고 오게** 해야 합니다. 사람이 만드는 한 언젠가 어긋납니다.

- **발행하지 않은 절이 생깁니다.** 절은 착수할 때도 세우므로, 그 사이 다음 버전이 나가면
  태그 없이 남습니다(v0.11.0 · v0.12.0). 묶어 지우지 않고 제목에 「발행 안 함」을 달아
  어느 릴리스에 함께 나갔는지 적기로 했습니다 — 무엇을 왜 만들었는지가 한 덩어리가 되면
  읽히지 않습니다.

---

## v0.13.0 — 화면은 두 말, 나머지는 영어 (2026-08-19)

**Apache-2.0 전환: 2030-08-19** (릴리스일로부터 4년)

**함께 나간 것** — v0.10.0 이후로 [v0.11.0](#v0110--화면의-바닥을-바꾼다-발행-안-함)(화면 기반을
chi · templ · htmx 로)과 [v0.12.0](#v0120--화면이-규칙을-고치고-세션을-연다-발행-안-함)(화면에서
규칙 편집 · 화면이 세션을 연다)이 이 릴리스에 담겼습니다. 그 둘은 따로 발행하지
않았습니다 — **`v0.10.0` 다음 태그가 `v0.13.0`인 것은 그래서입니다.**

**목표** — 문서는 한국어로 두되 **코드와 그 출력은 영어**가 기본이어야 합니다. 그리고
화면은 쓰는 사람이 **한국어와 English 중에 고를** 수 있어야 합니다.

### 만든 것

- **화면이 두 말을 씁니다.** 문구 200개가 `pkg/inventory/ui/text.go` 한 파일에 나란히
  있고, 토글은 이동 링크 오른쪽 끝에 있습니다. 말은 `?lang=` → 쿠키 →
  `Accept-Language` → 영어 순으로 고릅니다.

  **고른 것이 브라우저 설정을 이깁니다.** 그러지 않으면 토글을 눌러도 다음 화면에서
  되돌아가고, 토글이 있으나 마나가 됩니다. 말을 바꿔도 보던 자리에 남습니다 — 첫
  화면으로 되돌리면 표를 한참 내려온 사람은 말을 바꾸느니 그냥 참습니다.

- **명령의 출력·로그·오류를 영어로.** 두 리포에서 문자열 약 340개.

- **JSON 키 12개를 영어로**(호환 없이). `계층_판정` → `layer_decisions`,
  `확정될_정책` → `policy_on_finalize` 등. `변경` 칸의 값도 「추가」·「제거」에서
  `added`·`removed` 로 바꿨습니다 — **파일에 사람이 읽는 말을 담으면, 그 파일을 다른
  말로 연 화면에서 어긋납니다.**

- **`make check-text`** — 코드 문자열에 한글이 있으면 빌드가 붉어집니다.

### 알아낸 것

- **정규식으로는 못 잡습니다.** 즉석 스크립트로 세 번 훑고 「0개 남음」을 확인했는데,
  화면을 띄우니 `화면: http://...` 가 그대로 떠 있었습니다. 문자열 안의 `//` 에서 줄이
  잘린 것입니다. 파서로 다시 보니 **여러 줄 백틱 문자열 두 개**가 더 있었습니다
  (SQL 스키마 안의 주석 한 줄, 빈 선언 안내문). 그래서 게이트는 `go/ast` 로 봅니다.

- **어떤 문장은 화면과 명령 양쪽에 삽니다.** 확정 거절문, 선언의 문제 목록, 관측 경고가
  그렇습니다. 그냥 영어로 두면 한국어 화면에 영어가 섞이고, 한국어로 두면 명령이
  한국어입니다.

  문장이 아니라 **값**으로 만들고, 규칙을 하나 세웠습니다 — **영어는 그 사실을 가진
  패키지가 갖고, 한국어는 화면 카탈로그가 갖는다.** 영어를 두 곳에 두면 한쪽만
  고쳐지는 날이 오고, 그날 명령과 화면이 같은 문제를 다르게 설명합니다.

- **옮기지 않은 문구는 눈으로 못 찾습니다.** 그래서 화면을 통째로 영어로 그려 놓고
  한글이 한 글자라도 있으면 막습니다(IC-UI19). 토글만 예외입니다 — 지금 말을 못 읽는
  사람도 자기 말은 알아봐야 합니다.

- **주석은 한국어로 둡니다.** 「코드와 출력은 영어」의 범위를 식별자·출력·화면으로
  읽었습니다. 이 리포의 주석은 *왜 그렇게 했는지*를 담고 있어, 옮기면 그 판단이
  얇아집니다.

- **토폴로지 DOT 안의 글만 영어 하나로 둡니다.** 그림은 파일로도 나가는 산출물이고,
  그 파일을 누가 어느 말로 열지 우리가 알 수 없습니다.

---

## v0.12.0 — 화면이 규칙을 고치고 세션을 연다 (발행 안 함)

> **이 버전은 따로 발행하지 않았습니다.** 여기 적힌 것은 [v0.13.0](#v0130--화면은-두-말-나머지는-영어-2026-08-19)에
> 함께 나갔고, 라이선스 전환일도 그쪽을 따릅니다. 절을 남겨 둔 것은 **무엇을 왜
> 만들었는지가 한 덩어리로 묶이면 읽히지 않기** 때문입니다 — 태그가 없을 뿐 여기 적힌
> 일은 실제로 있었던 일입니다.

**목표** — v0.11.0의 목표를 여기서 끝냅니다. **5노드 사용자가 터미널을 열지 않고 한 바퀴를
됩니다.**

### 만든 것

- **자산 스코프 규칙을 화면에서 고칩니다.** 계층마다 다섯 칸짜리 표를 주고, **그 칸이 무슨
  뜻인지를 표 위에 둡니다.** 규칙을 적는 자리에서 읽히고, 화면이 바뀌면 같이 바뀝니다 —
  별도 문서로 미루면 둘이 갈라집니다.

  `action` 은 고르는 칸입니다. 오타로 규칙이 죽지 않습니다.

  **합본이 아니라 계층 파일을 고칩니다.** 합본에 쓰면 그 규칙이 조직에서 왔는지 노드군에서
  왔는지가 사라지고, 다음 리뷰에서 누구에게 물어야 할지 알 수 없게 됩니다.

- **화면이 세션을 엽니다.** `-layers` 를 주면 스코프 세션을, `-decl` 과 `-results` 를 주면
  리뷰 세션을 화면이 직접 만듭니다. 세우는 코드는 공용 패키지 한 곳이라
  (`review.FromResults`), 명령과 화면이 **같은 큐**를 봅니다.

  ```bash
  pqcaton-ui session.json -decl declaration.json -results results/ \
             -layers corp.csv,prod.csv -base asset-scope.csv -org acme
  ```

  세션 파일이 하나도 없는 상태에서 네 화면이 다 뜹니다.

- **재료가 바뀌면 세션도 다시 세우되, 적어 둔 판정은 따라옵니다**
  (`scope.Reopen` · `review.Carry`). 고칠 때마다 판정을 처음부터 다시 적게 하면 아무도
  화면을 쓰지 않습니다. 동일성이 열쇠입니다 — 규칙은 `RuleID`, 항목은 `ID`.

### 알아낸 것

- **판정을 따라오게 하는 것과 「본 적 없는 것을 승인으로 덮는 것」은 한 끗 차이입니다.**
  계층·정책에 **못 보던 변경이 생기면 그 일괄 결론을 지웁니다.** 일괄 판정은 「본 것들을
  보고 내린 결론」인데, 새 변경은 사람이 본 적이 없습니다. 그대로 두면 방금 넣은
  `exclude` 가 **누가 승인한 적 없는 근거를 달고** 확정을 통과합니다(IC-S11 · IC-Q6).

  정책·큐가 달라지면 **서명도 지웁니다.** 서명은 그 정책에 대한 것입니다. 승인자 이름은
  남깁니다 — 그건 사람이지 확인이 아닙니다.

- **빈 줄이 규칙이 되면 인벤토리가 통째로 빕니다.** pqcota는 빈 칸을 `*`로 읽습니다. 미리
  열어 둔 빈 줄에서 `action` 만 잘못 골라도 `exclude,*,*,*` 가 됩니다 — 오류도 나지 않고,
  다음 적재에서 자산이 0건이 됩니다. 그래서 **세 칸이 모두 빈 줄은 규칙으로 만들지
  않습니다.** 「전부」를 뜻하려면 `*`를 적습니다(IC-UI15).

- **문서로 미루는 것이 만드는 것보다 비쌀 때가 있습니다.** 「CSV를 이렇게 고치십시오」를
  쓰려면 다섯 칸의 뜻·glob·계층 우선순위를 다 가르쳐야 하고, 다 읽고도 손으로 CSV를 짜야
  합니다. 표를 편집 가능하게 하니 그 문서가 대부분 필요 없어졌습니다.

### 확인한 것

명령을 한 번도 돌리지 않고 화면만으로: 규칙 편집 → 계층 CSV → 판정·승인·확정 →
`asset-scope.csv` → **pqcota가 그 정책으로 관측 자산 6건 중 3건을 걸러냈습니다.**

```
자산 스코프: 관리 대상 아님으로 3건 제외(관측은 됐으나 적재 안 함)
  • node:48596282fd2faf23 자산 3 · 관측엣지 0
```

---

## v0.11.0 — 화면의 바닥을 바꾼다 (발행 안 함)

> **이 버전은 따로 발행하지 않았습니다.** 여기 적힌 것은 [v0.13.0](#v0130--화면은-두-말-나머지는-영어-2026-08-19)에
> 함께 나갔고, 라이선스 전환일도 그쪽을 따릅니다.

**목표** — 「자산 스코프 규칙을 CSV로 손수 고치라」고 안내하려면 다섯 열의 뜻과 glob 규칙과
계층 우선순위를 **문서로 가르쳐야** 합니다. 그 문서를 쓰는 것이 편집 화면을 만드는 것보다
비쌉니다. 화면을 두는 이유가 도구를 쓸 만하게 만드는 것인데, 정작 제일 아픈 자리를 파일
편집기로 미루고 있었습니다.

편집 기능을 얹기 전에 **바닥을 먼저 바꿉니다.** 둘을 한 번에 하면 무엇이 깨졌는지 가릴 수
없습니다. 이 버전은 **동작을 바꾸지 않습니다** — 기존 케이스가 그대로 통과하는 것이 그
증거입니다.

### 만든 것

- **라우팅을 chi로.** 예전에는 처리기마다 「POST 인가」를 손으로 물었고, 그 물음을 빠뜨린
  자리는 **GET 으로도 확정이 돌 수** 있었습니다. 이제 `r.Post` 로 등록하지 않은 주소에는
  POST 가 닿지 않습니다. Recoverer 를 둔 것은 리뷰 도중 서버가 죽으면 적던 판정이 통째로
  날아가기 때문입니다.

- **템플릿을 templ로.** 문자열 템플릿을 Go 코드로 옮겨 컴파일러가 봅니다.
  **생성물(`*_templ.go`)을 리포에 함께 둡니다** — 빌드에 생성기가 필요 없고, 이 리포를
  `go get` 하는 쪽도 도구를 깔지 않습니다. 고쳐 놓고 안 돌린 날은 `make verify-generated` 가
  잡습니다.

- **부분 갱신을 htmx로 — 「행 추가」에만.** 미리 열어 둔 빈 줄 세 개로 모자란 날, 예전에는
  저장하고 페이지를 다시 띄워야 했습니다. **그때 아직 저장하지 않은 편집이 날아갑니다.**

  새 줄과 「번호가 하나 오른 버튼」을 함께 돌려줍니다. 번호가 끊기면 `ApplyDecl` 이 거기서
  읽기를 멈춰 **그 뒤가 오류 없이 저장되지 않기** 때문입니다(IC-UI10).

  화면의 뼈대는 여전히 폼과 링크입니다 — 스크립트가 막힌 환경에서도 읽고 고칠 수 있습니다.

- **스타일과 htmx가 바이너리에 박혀 나갑니다.** CDN 을 걸지 않은 이유가 둘입니다: 관측 대상
  망이 바깥으로 못 나가는 것은 이 도구를 쓰는 곳에서 흔한 조건이고, 남의 서버에서 오는
  스크립트는 **우리 라이선스 게이트가 볼 수조차 없습니다.**

- **라이선스 게이트가 브라우저로 나가는 파일도 봅니다.** `go list -deps` 만 보던 게이트는
  배포물의 절반을 검사하지 않았습니다 — 「무엇을 쓰는지 모르면 이관도 못 한다」고 말하는
  리포에서. 이제 `.js`·`.css` 를 확장자로 훑고, `licenses.txt` 에 없는 파일은 통과하지
  못합니다. 값의 근거는 그 파일과 함께 받은 LICENSE 원문을 옆에 둡니다.

  ```
  ✓ 라이선스 검사 통과 (링크되는 모듈 14개, 실려 나가는 웹 자산 2개 — 전부 허용적)
  ```

### 알아낸 것

- **`vendor/` 같은 디렉터리 규약을 믿으면 게이트가 아닙니다.** 규약을 어긴 사람이 아니라
  **모르고 다른 곳에 놓은 사람**이 지나가게 됩니다. 그래서 확장자로 훑고, 우리가 쓴 파일도
  `BUSL-1.1` 로 적게 했습니다. 넓힌 첫날 게이트가 잡은 것이 **우리 CSS** 였습니다.

- **프런트를 어디까지 여느냐는 이 리포에서 남들보다 비쌉니다.** npm 을 들이면 의존성 수백
  개가 게이트 밖에 생깁니다 — 상업 라이선스 심사에서 제일 먼저 걸릴 자리입니다. 그래서
  단일 바이너리를 지키는 쪽(vendoring + Go 쪽 경량 프레임워크)을 골랐습니다. 링크되는
  모듈은 **둘만** 늘었고(templ · chi), 둘 다 전이 의존이 없습니다.

- **`go build ./...` 는 테스트를 컴파일하지 않습니다.** 라우팅을 바꾼 뒤 Mac 에서 빌드가
  초록이었는데, ubuntu-dev 에서 `make` 를 돌리자 명령 케이스 4곳이 옛 `routes(mux)` 를
  부르고 있었습니다. **검증은 `make` 로, 빌드 기계에서** 라는 규칙이 이번에도 값을 했습니다.

---

## v0.10.0 — 명령 하나를 지운다 (2026-08-19)

**Apache-2.0 전환: 2030-08-19** (릴리스일로부터 4년)

**목표** — 설계 문서를 처음부터 다시 읽다가, **`pqcaton-reconcile` 이 자리를 잃었다**는 것이
드러났습니다. 하던 일이 `pqcaton-decide open`(지름길)의 **부분집합**입니다.

### 고친 것

- **`pqcaton-reconcile` 을 지웠습니다.** v0.1.0에 「대조를 눈으로 보는 명령」으로 두었는데,
  그 뒤 `report`·`decide`·`ui` 가 생기면서 남은 차이가 **콘솔 표 하나**뿐이었습니다.
  그것만 **`pqcaton-decide open -view`** 로 옮겼습니다.

  ```bash
  pqcaton-decide open decl.csv -view > session.json   # 표는 표준오류, 세션은 표준출력
  ```

  **표를 표준오류로 찍는 것**이 중요합니다 — 세션 JSON 이 표준출력으로 나가므로 섞이면
  파이프가 깨집니다. `-results` 경로에서도 같이 씁니다.

- **설계 문서의 틀린 참조를 고쳤습니다.**

  | 무엇 | 어떻게 |
  |---|---|
  | `pqcota(pqcota pkg/org)` | 「상류」 일괄 치환이 남긴 자국(v0.5.0 뒤) |
  | `§2.4 상태기계` · `§2.5 판정 영속` · `§2.1 대조` | **이 문서 자신의 옛 번호**인데 pqcota 규정서를 가리키는 것으로 읽혔습니다 — 따라가면 「정규화 파이프라인」·「자동화 규정」·「목적과 경계」가 나왔습니다. 지금 번호(§1.4·§1.5·§1.1)로 고치고 문서 안 링크를 걸었습니다 |
  | 겹친 수평선 | v0.3.0 편집이 남긴 것 |
  | 구어체 (`더하는 건`·`이건`·`어떻게 다뤘나`) | 이 문서의 `것입니다`체로 |

### 알아낸 것

- **명령이 늘 때는 보이지 않고, 문서를 통으로 읽을 때 보입니다.** `reconcile` 은 그때그때
  고쳐지며 살아남았지만 **케이스가 하나도 없었고** 로컬 스캔 코드를 두 곳에서 부르게
  했습니다(v0.8.1에서 `localscan` 으로 모은 이유가 그것이었습니다).
- **§ 참조는 문서를 옮기거나 절 번호를 바꿀 때 조용히 썩습니다.** 「§ 표기는 pqcota 규정서」라고
  선언해 둔 문서에서 자기 절을 § 로 가리키면 **다른 문서를 가리키게 됩니다.** 문서 안을
  가리킬 때는 링크를 걸어야 합니다.

---

## v0.9.0 — 관측한 노드를 판정으로 잇는다 (2026-08-19)

**Apache-2.0 전환: 2030-08-19** (릴리스일로부터 4년)

**목표** — **주경로가 끊겨 있었습니다.** 여러 노드를 관측해 대조까지 해 놓고도, 그 결과를
리뷰 큐에 올릴 길이 없었습니다. 확정 계획을 만들 수 있는 것은 **명령을 실행한 그 기계 하나**뿐이라
v0.1.0이 내건 *"관측을 판정으로 잇는다"* 가 코드로는 거짓이었습니다.

### 만든 것

- **`pqcaton-decide open -results <dir>`** — pqcota 가 모은 관측으로 리뷰 세션을 만듭니다.
  대조는 [`report`](pkg/inventory/report) 가 하므로 **대조 화면이 보는 것과 같은 계산**입니다 —
  화면에서 본 shadow 가 리뷰 큐에 그대로 올라옵니다.
- **`delta` 도 같은 입력을 받습니다.** 근거를 다시 만드는 경로가 `open` 과 갈리면 델타 리뷰가
  「바뀌지 않은 것」을 바뀌었다고 부릅니다.
- **선언이 조직을 말합니다.** `-org` 를 따로 주지 않으면 선언의 것을 쓰고, 둘이 어긋나면 끊습니다.
- **데모가 확정까지 갑니다** — `report` 다음에 `decide open -results` → `close` 를 이어 붙였습니다.

### 알아낸 것

- **데모가 대조에서 멈춰서 아무도 못 봤습니다.** `extend.sh` 는 `pqcaton-report` 까지만 돌리고,
  판정은 로컬 스캔 경로로 따로 했습니다 — **두 길이 만난 적이 없어서** 그 사이가 비어 있다는
  것이 드러나지 않았습니다. 이제 데모가 그 자리를 지킵니다.
- **끝까지 이어 봐야 이어진 것입니다.** 데모 3노드로 돌려 확정 계획이 `pay-app`·`web-gw` 를
  겨누는 것을 확인하고, 그 계획을 `pqcota-provision` 에 먹여 **실제 Ansible 플레이북**이
  나오는 것까지 봤습니다.

### 고친 것

앞 버전에서 잘못 나온 것은 없습니다 — 다만 **낼 수 없던 것이 있었습니다.**

---

## v0.8.1 — 관측하지 못한 것을 「없다」로 말하지 않는다 (2026-08-19)

**Apache-2.0 전환: 2030-08-19** (릴리스일로부터 4년)

**목표** — 오류 없이 틀린 답을 내던 자리 셋을 닫습니다. 기능은 더하지 않습니다.

### 고친 것

- **비-리눅스에서 로컬 스캔이 「관측 완전」인 척했습니다.** **v0.1.0부터**입니다.

  pqcota 가 `ProcUnavailable` 로 *"`/proc` 을 못 열었다"* 를 알려 주는데 **그 플래그를 아무도
  안 봤습니다.** macOS·Windows 에서 `pqcaton-decide open` 을 돌리면 「접근가능 0」만 찍히고,
  선언한 자산이 전부 `UNOBSERVED` 로 나오며, 리포트와 화면은 **「못 본 것: 없습니다 — 이
  범위에서는 관측이 완전합니다」** 라고까지 말했습니다. **관측을 아예 못 한 기계에서.**

  이제 끊고 무엇을 대신 해야 하는지 말합니다. `/proc` 은 열렸는데 하나도 못 읽은 경우(권한)는
  결과는 보여 주되 **「없는 것이 아니라 못 본 것」**이라고 말합니다 — 끊는 것과 경고하는 것을 가릅니다.

- **노드 인자가 이름표일 뿐이라는 것을 말하지 않았습니다.** **v0.1.0부터**입니다.

  `pqcaton-decide open decl.csv web-gw` 는 `web-gw` 를 관측하지 않습니다. **이 기계를 관측해
  `web-gw` 라고 적습니다.** usage 가 `[node]` 라고만 적어 「그 노드를 관측한다」로 읽혔고,
  읽히는 대로 쓰면 **다른 기계의 관측이 `web-gw` 의 것으로 CONFIRMED 가 됩니다.** 이제
  경고하고, usage 도 `[이-기계에-붙일-이름]` 으로 바꿨습니다. 막지는 않습니다 — 데모가 쓰는 길입니다.

- **문서에 사전 준비가 없었습니다.** 화면이 *"`dot` 으로 그리십시오"* 라고 말하는데 `dot` 안내가
  어디에도 없었습니다. README 에 「사전 준비」를 두고, 화면·명령의 문구가 **「선택 사항」임을
  먼저** 말하도록 고쳤습니다. **어디서 실행되는가**(ctl 노드는 OS 무관 · 관측은 Linux 대상 노드)도
  함께 적었습니다.

### 알아낸 것

- **두 명령이 같은 스캔을 중복하고 있었습니다.** 고치려면 두 곳을 고쳐야 했습니다 —
  [`pkg/inventory/localscan`](pkg/inventory/localscan)으로 모았습니다.
- **`pqcaton-decide open` 은 ctl 노드의 일이 아닙니다.** collector 가 할 일을 로컬에서 흉내 낸
  지름길이고, 그래서 Linux 조건을 물려받습니다. 제품으로서의 pqcaton 은 ctl 노드에서 돌고
  **OS 를 가리지 않습니다.**

---

## v0.8.0 — 무엇을 볼지도 화면에서 정한다 (2026-08-19)

**Apache-2.0 전환: 2030-08-19** (릴리스일로부터 4년)

**목표** — 화면이 셋이 됐는데 **자산 스코프만 CLI**로 남아 있었습니다. 「이 자산은 안 본다」는
사고 뒤에 근거를 대야 하는 결정이라, 가장 화면이 필요한 자리이기도 합니다.

### 만든 것

- **자산 스코프 화면** — 변경을 **계층으로 묶어** 보여 주고 계층마다 결론 칸 하나를 줍니다.
  `exclude` 추가만 **근거 필수**라는 무게 차이가 화면에 드러납니다. 확정될 정책은 **전문**으로
  보여 줍니다 — 바뀐 것만 리뷰하되 나가는 것은 전문입니다.
- **세션 형식과 확정 게이트를 공용으로**([`pkg/inventory/scope`](pkg/inventory/scope)) —
  `pqcaton-scope`의 `package main` 안에만 있어 화면이 손댈 수 없었습니다. 선언·리뷰와 같은
  순서입니다.
- **탭이 절차 순서로 완성됐습니다** — ① 선언 → ② 스코프 → ③ 대조 → ④ 리뷰 큐.
  **무엇을 계속 볼지가 정해져야 관측이 적재되므로** 스코프가 대조 앞입니다.

### 알아낸 것

- **화면 넷이 모두 같은 모양을 갖게 됐습니다** — 묶어서 보이고(정책·계층), 묶음마다 결론
  하나를 주고, 막히면 무엇이 남았는지 말합니다. 세 번 같은 것을 만들고 나서야 그것이 이
  도구의 기본 동작이라는 것이 분명해졌습니다.
- **`pqcaton-scope` 를 옮기다 파일을 깨뜨렸습니다.** 정규식으로 함수를 지우려다 주석까지
  같이 걷어 갔습니다 — 되돌리고 통째로 다시 썼습니다. 옮기는 일은 지우는 것보다 **다시
  쓰는 편이 쌉니다.**

### 고친 것

앞 버전에서 잘못 나온 것은 없습니다.

---

## v0.7.0 — 대조 결과를 화면에서 본다 (2026-08-19)

**Apache-2.0 전환: 2030-08-19** (릴리스일로부터 4년)

**목표** — 선언을 화면에서 쓸 수 있게 됐는데, **그 결과를 보려면 다시 터미널**이었습니다.
그리고 화면 탭이 절차와 다른 순서로 놓여 있었습니다.

### 만든 것

- **대조 결과 화면** — ① 관측(누가 무엇을 봤나 · **못 본 것**) · ② 자산 3-상태 · ③ 통신 엣지와
  양자내성 등급 · ④ 거버넌스 토폴로지. **관측을 먼저 보입니다** — 그것 없이는 UNOBSERVED가
  「없다」인지 「원리상 못 봤다」인지 읽는 사람이 가를 수 없습니다(§2.7).
- **탭이 절차 순서입니다** — ① 선언 → ② 대조 → ③ 리뷰 큐. 쓰는 사람이 다음에 무엇을 할지
  **순서 자체가 말해 줍니다.** 재료를 주지 않은 자리는 만들지 않습니다.
- **대조 계산을 공용 패키지로**([`pkg/inventory/report`](pkg/inventory/report)) — 명령은 글로
  찍고 화면은 표로 그리되 **계산은 하나**입니다. 계산이 두 곳에 있으면 화면과 글이 다른 답을 내는 날이
  오고, 그때 어느 쪽이 맞는지 아무도 모릅니다.
- **필수 리뷰가 위로** — 자산 표에서 UNDECLARED·UNOBSERVED를 먼저 보입니다. 자동으로 넘어가는
  CONFIRMED가 위에 오면 정작 볼 것이 아래로 밀립니다.

### 알아낸 것

- **`dot` 은 의존성이 아니라 있으면 좋은 것입니다.** 있으면 토폴로지를 SVG로 그리고, 없으면
  DOT 원문을 보입니다 — 없다고 화면이 깨지면 「표준 라이브러리만 쓴다」는 약속이 무의미해집니다.
- **`pqcaton-report` 안에 사본이 넷 남아 있었습니다.** 계산을 패키지로 뺐는데 명령에 옛
  함수가 그대로 있었습니다 — 안 쓰이는 코드라 컴파일은 통과합니다. 지웠습니다.

### 고친 것

- **확정 계획 케이스가 바이트 비교였습니다.** `protojson` 은 콜론 뒤 공백을 **일부러 흔들어**
  바이트 비교를 막는데, 그렇게 재면 통과 여부가 운에 달립니다. **v0.5.0부터** 그랬고,
  지금까지 우연히 통과하고 있었습니다 — 파싱해서 재도록 고쳤습니다. 잘못 나온 계획은 없습니다.
- **케이스 번호 `IC-N1`~`N3` 이 `IC-R8`~`R10` 으로 옮겼습니다.** 재던 함수가 명령에서 패키지로
  갔기 때문입니다 — 재는 자리가 옮겨졌으니 번호도 그 절로 갑니다.

---

## v0.6.0 — 선언을 화면에서 쓴다 (2026-08-19)

**Apache-2.0 전환: 2030-08-19** (릴리스일로부터 4년)

**목표** — 대조는 선언과 관측을 맞대는 일인데, **한쪽 레인이 통째로 손입력**이었습니다.
`declaration.json`을 편집기로 적어야 했고, 거기가 틀리면 대조 결과가 **오류 없이** 틀립니다.

### 만든 것

- **선언 편집 화면** — 조직 · 스코프 · **관측 대상 노드(이름·IP)** · 자산 · 통신 엣지.
  한 노드가 망 둘에 걸치면 IP도 둘인데, 쉼표로도 공백으로도 받습니다.
- **선언 자체 검사**([`pkg/inventory/decl`](pkg/inventory/decl)) — 저장 전에 짚습니다.
  IP 없는 노드 · 겹친 IP · IP 아닌 값 · 스코프 밖을 가리키는 자산/엣지 · 포트 0.
  **막지는 않습니다** — 아직 IP를 모르는 노드를 적어 두는 것도 정당한 상태입니다.
- **화면을 공용 패키지로**([`pkg/inventory/ui`](pkg/inventory/ui)) — 그리는 것과 폼을 읽는
  것만 갖습니다. **두 배포 형태가 같은 화면을 씁니다** — 어디서 읽고 누가 들어오는지만
  부르는 쪽이 정합니다.
- **선언 형식도 공용으로**([`pkg/inventory/decl`](pkg/inventory/decl)) —
  `pqcaton-report`의 `package main` 안에만 있어 화면이 손댈 수 없었습니다.

### 알아낸 것

- **화면을 명령 안에 두면 밖에서 못 씁니다.** `package main`의 소문자 이름은 import 자체가
  안 됩니다 — 「전제가 달라서 못 옮긴다」보다 앞에 있는 벽이었습니다. 새 화면을 또 거기
  넣으면 옮길 것이 네 배가 되므로, **첫 화면을 만들면서 함께 뺐습니다.**
- **같은 결과를 두 자리에서 다르게 말하고 있었습니다.** 「IP가 없다」와 「IP 표에 아예 없다」는
  결과가 같은데 한쪽만 약하게 적었습니다 — 케이스가 잡았습니다. 고칠 이유의 무게가 달라
  보이면 사람은 약한 쪽을 미룹니다.
- **`pqcaton-report`도 이제 선언을 검사합니다.** 화면을 안 쓰는 사람에게도 같은 경고가
  갑니다 — 검사가 화면에만 있으면 CLI 사용자는 계속 오류 없이 틀립니다.

### 고친 것

앞 버전에서 잘못 나온 것은 없습니다.

---

## v0.5.0 — 리뷰 큐를 화면에서 다룬다 (2026-08-18)

**Apache-2.0 전환: 2030-08-11** (이 버전은 고정 Change Date 로 배포됐습니다)

**목표** — 지금까지 판정은 JSON을 손으로 채우는 일이었습니다. 항목이 수십 개면 눈으로 훑게
되고, 눈으로 훑으면 빠뜨립니다. 그 자리를 화면으로 올리되 **확정의 무게는 그대로** 둡니다.

### 만든 것

- **리뷰 화면**([`pqcaton-ui`](inventory/cmd/pqcaton-ui)) — 세션 파일을 열어 **정책 단위로**
  결론을 채우고, 확정 버튼이 `pqcaton-decide close` 와 **같은 게이트**를 탑니다. 확정되지
  않으면 무엇이 남았는지 화면에 그대로 보입니다.
- **확정 게이트를 공용 패키지로**([`pkg/inventory/review`](pkg/inventory/review)) — 파일
  형식과 `Finalize` 를 명령에서 떼어 명령과 화면이 그 하나를 씁니다.
- **의존성이 늘지 않습니다** — `net/http` 와 `html/template` 만 씁니다. 자바스크립트도 없습니다.
- **기본은 `127.0.0.1`** 이고, 루프백이 아니면 경고합니다.

### 알아낸 것

- **게이트를 복붙했다면 언젠가 갈렸을 것입니다.** 화면과 명령이 각자 게이트를 들면 한쪽만
  고쳐지는 날이 오고, 그날 화면으로는 확정되는데 명령으로는 안 되는 계획이 생깁니다. 그래서
  화면을 만들기 전에 게이트를 먼저 뺐습니다.
- **화면이 생겨도 산출물은 파일입니다.** 편집한 세션 파일 · 확정 계획 · 판정 원장이 그대로
  남습니다 — 무엇을 근거로 무엇을 정했는지가 화면에서 사라지지 않습니다.
- **리뷰 큐는 그 조직의 공격면입니다.** 어느 노드가 무엇을 쓰는지가 그대로 있어, 실수로 밖에
  열리는 쪽이 편의보다 훨씬 비쌉니다.

### 고친 것

- **화면에서 위치 인자 뒤의 플래그가 무시됐습니다.** 표준 `flag` 는 첫 비플래그에서 파싱을
  멈추므로 `pqcaton-ui session.json -addr ...` 의 `-addr` 이 **조용히 무시되고** 기본 주소로
  떴습니다. 밖으로 열려던 값도, 안쪽으로 좁히려던 값도 안 먹는 자리였습니다. 다른 명령들과
  같은 규칙으로 맞췄습니다 — **케이스가 아니라 실제로 띄워 보다 드러났습니다.**
- **`Pending` 이 정책 판정을 보지 않았습니다.** 정책으로 이미 판정된 항목까지 「결론 없음」으로
  세어, 다른 이유로 막혔을 때 엉뚱한 것을 고치게 했습니다. 잘못 나온 계획은 없습니다.

---

## v0.4.0 — 격리를 DB가 강제한다 (2026-08-18)

**Apache-2.0 전환: 2030-08-11** (이 버전은 고정 Change Date 로 배포됐습니다)

**목표** — 조직 격리를 한 겹 더 겁니다. 지금까지 격리는 **질의마다 `org`를 다는 것**으로
서 있었는데, 그것은 우리가 안 틀린다는 전제 위에 있습니다. 여러 조직이 한 데이터베이스를
쓰는 배포에서는 그 전제 하나에 전부를 걸 수 없습니다 — 조건을 빠뜨린 질의가 언젠가
들어옵니다.

### 만든 것

- **판정 원장에 Postgres RLS** — 정책이 `org = current_setting('pqcaton.org')`로 행을 가릅니다.
  **읽기와 쓰기를 함께 막습니다**(`USING` + `WITH CHECK`).
- **`FORCE ROW LEVEL SECURITY`** — 없으면 테이블 소유자가 예외가 됩니다. 대개 앱이 소유자로
  붙으므로, 그 한 줄이 없으면 **정책을 걸어 놓고도 아무 일도 일어나지 않습니다.**
- **무력한 연결을 잡아냅니다** — 슈퍼유저와 `BYPASSRLS` 롤은 정책을 통째로 건너뜁니다. 열 때
  한 번 재서 `RLSActive()`로 들고 다니고, **`PQCATON_REQUIRE_RLS=1`이면 열지 않습니다.**
  pqcota의 `PQCOTA_REQUIRE_SIGNATURE`와 같은 모양이라 두 리포를 오가는 사람이 같은 자리에서
  같은 것을 찾습니다.
- **DDL 권한 없이도 열립니다** — RLS가 실제로 물게 하려면 앱이 테이블 소유자로 붙지 않아야
  하고, 그러면 그 연결에 DDL 권한이 없는 것이 정상입니다. 이미 갖춰져 있으면 넘어가고,
  아니면 **무엇을 소유자로 돌려야 하는지 말하고 멈춥니다.**

### 알아낸 것

- **정책을 거는 것과 정책이 무는 것은 다릅니다.** 슈퍼유저로 붙으면 위 SQL을 전부 돌려도
  아무것도 막히지 않습니다 — 가장 위험한 종류의 거짓 안심입니다. 그래서 케이스가
  **`NOSUPERUSER NOBYPASSRLS` 롤을 만들어 그 롤로 다시 붙습니다.** 슈퍼유저 조건에서
  통과하는 RLS 케이스는 아무것도 재지 못합니다.
- **읽기만 막으면 오염을 막지 못합니다.** `WITH CHECK`가 없으면 남의 조직 이름으로 적은
  INSERT가 그대로 들어가고, 그 행은 정작 우리 눈에는 안 보입니다.
- **테이블이 있다고 정책이 있는 것은 아닙니다.** 스키마 확인을 테이블 존재로만 하면 정책
  없는 테이블에 조용히 붙고, 한 겹이 없다는 사실을 아무도 모릅니다 — `relrowsecurity`까지
  봅니다.

### 고친 것

앞 버전에서 잘못 나온 것은 없습니다.

---

## v0.3.0 — 무엇을 안 볼지도 승인을 거친다 (2026-08-18)

**Apache-2.0 전환: 2030-08-11** (이 버전은 고정 Change Date 로 배포됐습니다)

**목표** — 「이 자산은 안 본다」는 결정을 리뷰-확정 상태기계에 태웁니다. 혼자 쓰면 자기
책임이지만 조직은 누가·언제·왜 뺐는지 남겨야 합니다 — 사고 뒤에 *"왜 이게 인벤토리에
없었나"* 에 답해야 하기 때문입니다.

**규칙을 다시 만들지 않습니다.** 형식(`scope.AssetPolicy`)과 집행(`pqcota-ingest -scope-assets`)은
pqcota가 갖습니다. 이 버전은 그 위의 거버넌스만 얹습니다.

### 만든 것

- **계층 상속**([`pkg/inventory/scope`](pkg/inventory/scope)) — 조직 → 환경 → 노드군을 준
  순서대로 겹칩니다. **상속 규칙을 따로 만들지 않았습니다** — pqcota가 이미 "매치되는 마지막
  규칙이 결정"이라 정해 두었으므로 상위를 앞, 하위를 뒤에 두면 그대로 하위 우선이 됩니다.
  판정 규칙이 세상에 하나만 존재합니다.
- **변경 승인**([`pqcaton-scope`](inventory/cmd/pqcaton-scope)) — 계층을 겹쳐 **바뀐 규칙만**
  리뷰에 올리고, `pqcaton-decide`와 같은 파일 왕복으로 승인합니다. **`exclude` 추가는 결론이
  없으면 확정되지 않습니다.** 제외를 거두는 쪽은 인벤토리가 넓어지는 방향이라 무게가 다릅니다.
- **계층 단위 일괄 판정** — 계층 하나에 결론 하나가 기본입니다. 규칙 한 줄씩 승인하는 리뷰는
  수천 대에서 끝나지 않습니다.
- **감사 기록** — 확정된 변경이 판정 원장에 append-only로 남습니다. 게이트를 지난 뒤에만
  남깁니다 — 확정되지 않은 것을 쌓으면 그 기록이 "누가 무엇을 승인했나"를 더는 답하지 못합니다.
- **정책 배포** — 확정되면 **pqcota의 집행기가 읽는 CSV 그대로** 나갑니다. 우리 형식을 만들면
  「거버넌스가 확정한 정책을 pqcota가 집행한다」가 코드로는 거짓이 됩니다.
- **제외분 재검토**(`pqcaton-scope review`) — 정책이 뺀 자산을 **이름으로** 보여 주고, 승인이
  아예 없거나 만료된 것만 다시 올립니다. **제외는 영구 면제가 아닙니다.**

### 알아낸 것

- **pqcota는 제외분을 수로만 셉니다**(`Snapshot.ExcludedByScope`). 잡음을 거르는 것이 목적이라
  그것으로 충분합니다. 거버넌스는 셈이 아니라 이름이 필요합니다 — 그래서 pqcota의 `Managed`를
  **그대로 호출해** 무엇이 빠졌는지 이름을 붙입니다. glob을 다시 구현하면 내려보낸 CSV를
  pqcota가 집행한 결과와 우리 화면이 갈라집니다.
- **「제외 ≠ 부재」는 적재 시점에만 참이었습니다.** 한 번 뺀 자산이 그대로 잊히면 그 사이
  위험해져도 인벤토리는 아무 말도 하지 않습니다 — 시간 축에서는 거짓이 되는 자리입니다.
  재검토가 그것을 메웁니다.
- **하위 계층이 상위의 제외를 되돌릴 수 있습니다.** 상속을 이어붙이기로 두었기 때문입니다.
  그것을 막는 자리는 상속 규칙이 아니라 **리뷰-확정 게이트**입니다 — 하위 정책 변경도 승인을
  거칩니다. 규칙을 두 곳에 두는 것보다 게이트 하나를 믿는 편이 낫습니다.

### 고친 것

앞 버전에서 잘못 나온 것은 없습니다.

---

## v0.2.0 — 대조가 조직에 묶인다 (2026-08-18)

**Apache-2.0 전환: 2030-08-11** (이 버전은 고정 Change Date 로 배포됐습니다)

**목표** — 판정 저장소만 조직에 묶여 있고 **대조 엔진은 아니었습니다.** 한 프로세스가 여러
조직을 다루는 배포에서 조직을 섞지 않을 책임이 부르는 쪽으로 통째로 넘어가 있었습니다.
그 책임을 엔진으로 되가져옵니다.

### 만든 것

- **조직에 묶인 대조 엔진** — `reconcile.For(조직)`으로 열고, 그 핸들로만 대조합니다.
  **다른 조직의 자산이나 엣지가 입력에 하나라도 섞이면 대조하지 않고 끊습니다**(`ErrOrgMismatch`).
  빈 조직으로는 엔진이 열리지 않습니다 — 판정 저장소와 같은 규칙입니다.
- **`AssetKey`·`EdgeKey`가 조직을 듭니다.** 조직이 동일성의 일부입니다 — 노드 이름이 같아도
  조직이 다르면 다른 자산입니다.
- **조직을 찍는 자리를 엔진 하나로 몰았습니다.** 스냅샷에서 관측 자산을 뽑는 것도, 선언에서
  자산을 뽑는 것도 엔진을 거칩니다. 대조 함수 자체는 패키지 밖으로 내보내지 않습니다 —
  남겨 두면 검사를 건너뛰는 길이 열린 채로 남습니다.
- **선언 문서가 조직을 말합니다** — `declaration.json`의 `org`. 선언은 고객의 문서이므로
  거기 적히는 것이 맞습니다. 비우면 `local`입니다.

### 알아낸 것

- **엔진은 조직을 스스로 알아낼 수 없습니다.** pqcota의 `history.Snapshot`에도 계약의
  `Envelope`에도 조직 필드가 없습니다(`target_node_id`·`scope_master_ref`까지입니다).
  그러니 조직은 **부르는 쪽이 단언**할 수밖에 없고, 이 버전이 막는 것은 바로 그 단언이
  틀렸을 때입니다.
- **섞인 입력은 오류로 드러나지 않습니다.** 조직이 열쇠의 일부라 그냥 두면 같은 자산이
  `UNDECLARED`와 `UNOBSERVED` 한 쌍으로 갈리고, **리뷰 큐가 그것을 shadow 발견으로 올립니다.**
  틀린 답이 아니라 **그럴듯한 답**이 나오는 것이 검사를 대조보다 먼저 두는 이유입니다.
- **판정 원장의 대상 id에는 조직을 넣지 않았습니다.** 원장이 이미 조직별로 갈려 있어
  중복이고, 넣으면 v0.1.x에 쌓인 판정의 대상이 전부 어긋납니다. 실제로 v0.1.x 원장을 새
  코드로 돌려 `BasisHash`가 그대로임을 확인했습니다.

### 고친 것

- **v0.1.1이 붙인 케이스 번호가 겹쳤습니다.** 계약 형식 케이스를 `IC-C1`·`IC-C2`로 달았는데
  그 번호는 confidence 절이 이미 쓰고 있었습니다. `IC-P6`·`IC-P7`로 옮겼습니다 — 문서만
  어긋났고 코드가 잘못 나온 것은 없습니다.

  **아래 v0.1.1 절은 그대로 둡니다**(발행된 절은 고치지 않습니다). 거기 적힌
  `IC-C1`·`IC-C2`가 지금의 `IC-P6`·`IC-P7`입니다.

---

## v0.1.1 — 확정 계획이 겨누는 노드를 바로잡는다 (2026-08-18)

**Apache-2.0 전환: 2030-08-11** (이 버전은 고정 Change Date 로 배포됐습니다)

**목표** — v0.1.0을 공개한 직후 검증에서 드러난 결함 하나를 고칩니다. 기능은 더하지 않습니다.

### 고친 것

- **확정 계획의 `target_node_id`가 틀렸습니다.** **v0.1.0부터**입니다.

  항목 id에서 노드를 되찾으려고 `/`로 쪼갰는데, 스코프가 `host://local` 같은 URI면 첫 조각이
  `host:`가 됩니다. **조치가 있지도 않은 노드를 겨눴습니다.** 같은 자리에서 런타임도 빈
  문자열이 되어 기본값 `openssl`로 떨어졌습니다 — **관측이 JCA여도 OpenSSL로 나갔습니다.**
  데모 자산이 전부 openssl이라 결과가 맞아 보였을 뿐입니다.

  되찾는 대신, 대조할 때 이미 알던 `node`·`runtime`을 세션 파일에 적어 그대로 들고 갑니다.
  쪼개는 함수 자체가 사라집니다 — 되찾을 것이 없으면 되찾다 틀릴 일도 없습니다.

  **v0.1.0이 만든 세션 파일에는 그 두 필드가 없습니다.** 빈 채로 통과시키면 이름 없는 노드에
  조치가 걸리므로, 확정 직전에 끊고 `open`을 다시 돌리라고 말합니다. 이미 확정해 둔 계획이
  있으면 `target_node_id`를 확인하십시오 — `host:` 같은 값이면 다시 내야 합니다.

- **`delta`가 빈 결과에 `null`을 돌려줬습니다.** 받는 쪽이 길이를 셀 수 없었습니다. 이제 `[]`를 돌려줍니다.
- 조직 불일치 메시지의 조사를 고쳤습니다.

### 알아낸 것

- **`toContract`에 케이스가 없었습니다.** 계약 형식으로 옮기는 마지막 단계라 여기가
  틀리면 앞의 판정이 다 옳아도 엉뚱한 노드에 적용됩니다. `IC-C1`·`IC-C2`로 답니다
  ([검증 기준](docs/testcases.md)). **pqcota에 먹여 플레이북이 나오는 것만 봤지, 겨눈 노드
  이름이 옳은지는 보지 않았습니다** — 형식이 맞는 것과 값이 맞는 것은 다릅니다.

---

## v0.1.0 — 관측을 판정으로 잇는다 (2026-08-18)

**Apache-2.0 전환: 2030-08-11** (이 버전은 고정 Change Date 로 배포됐습니다)

**목표** — **체크아웃해서 한 바퀴를 돌 수 있게** 합니다. 이 머신을 관측해 선언과 대조하고,
리뷰 큐를 사람이 판정하고, 확정한 계획이 pqcota 프로비저닝의 입력이 되는 데까지 — 명령 셋으로
끝납니다. 그리고 그 확정 없이는 적용이 돌지 않게 막습니다.

### 만든 것

- **3-상태 대조 엔진**([`pkg/inventory/reconcile`](pkg/inventory/reconcile)) — 선언과 관측을
  맞대어 `CONFIRMED`(선언∩관측) · `UNDECLARED`(관측만 = shadow) · `UNOBSERVED`(선언만)로
  가릅니다. **`UNDECLARED`가 이 도구의 첫 값입니다** — CMDB에 없는데 실제로 통신하고 있는
  엣지가 거기 있습니다.
- **confidence 스코어링** — 관측 빈도·기간·선언 신선도·소스 일치도·`evidence_strength`로
  정합니다. `inferred-low`는 상한을 누릅니다 — **약한 근거가 높은 신뢰도로 올라가지 못하게**
  하는 자리입니다.
- **리뷰 큐** — 자동통과 후보와 필수 개별 리뷰로 가르고 `위험도 × 블라스트반경 × 데이터민감도`로
  줄을 세웁니다. **승인은 사람이 합니다.**
- **리뷰–확정 상태기계**([`pkg/inventory/decision`](pkg/inventory/decision)) —
  `draft → in-review → finalized`. **finalized 아니면 적용이 거부됩니다**(최강 게이트).
  링·도메인 단위 부분 확정은 허용합니다.
- **판정 영속화** — append-only. 판정은 엣지 상태가 아니라 **사람의 결론**이라 재수집으로
  관측이 바뀌어도 대상에 붙어 남습니다. `BasisHash`가 근거를 추적하고, 근거가 실질적으로
  바뀌면 **그 판정만** 재검토 플래그를 답니다(델타 리뷰).
- **판정·확정 명령**([`pqcaton-decide`](inventory/cmd/pqcaton-decide)) — 리뷰 큐를 세션
  파일로 만들고, 사람이 결론·승인자·서명을 채워 넣으면 확정합니다. 하나라도 비면 **확정하지
  않고 무엇이 남았는지 말합니다.** 나온 확정 계획은 **계약 형식**이라 `pqcota-provision`이
  그대로 받습니다 — 우리 형식을 따로 만들지 않습니다.
- **판정 영속화**([`FileJudgmentStore`](pkg/inventory/decision/file.go)) — 파일 한 줄에 판정
  하나(JSONL), `O_APPEND`로만 엽니다. **Postgres 없이 체크아웃만으로 판정이 남습니다** —
  DB를 세우게 하면 거기서 멈추고, 안 남기면 「감사 근거로 남는다」가 명령줄에서 거짓이 됩니다.
- **정책 단위 일괄 판정** — 정책 하나에 결론 하나가 기본이고 개별 판정은 예외입니다(§3.4).
  버전 해시가 붙은 같은 라이브러리들이 한 정책으로 묶입니다.
- **델타 리뷰**(`pqcaton-decide delta`) — 쌓인 판정을 지금 관측과 대조해 **근거가 바뀐 것만**
  골라 줍니다. 재관측할 때마다 전부 다시 보게 하면 아무도 안 봅니다.
- **거버넌스 토폴로지** — 색이 양자내성 등급, 선형이 대조 상태입니다. 미관측을 점선으로 그려
  **부재와 구분합니다.**
- **러너**([`saas/runner`](saas/runner)) — 호스팅으로 쓸 때 고객망에서 실행되는 「올리는 입」.
  스케줄에 깨어나 플레이북을 돌리고, 연결확인과 관측 결과를 각각 올립니다. **주소는 조직
  단위 키로 토큰을 만들고 원본을 버립니다** — 밖으로 나가는 구조체에 주소 필드가 없습니다.
- **명령 셋** — `pqcaton-reconcile`(로컬 대조) · `pqcaton-report`(여러 노드 + 토폴로지) ·
  `pqcaton-decide`(판정·확정·델타). **체크아웃만으로 처음부터 끝까지 해볼 수 있습니다** — pqcota 데모도
  Postgres도 없이 이 머신을 관측해 확정 계획까지 만듭니다.
- **문서** — [여정](docs/journey.md)(자기 서버에 설치해 쓰는 길) ·
  [구조 그림](https://www.sntsoft.co.kr/pqcaton/) · [설계](docs/design.md) ·
  [검증 기준](docs/testcases.md). 케이스 54개(대조 12 · 판정 24 · 러너 17 · 계약 소비 1).

### 고친 것

첫 릴리스라 고칠 앞 버전이 없습니다.

### 기반 프로젝트 의존

pqcota **v0.5.0**. `go.mod`에 `replace`가 없습니다 — `go get` 한 줄로 시작합니다.
